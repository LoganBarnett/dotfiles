################################################################################
# This module enforces LDAP authentication via Basic Authentication
# (basicauthenforces LDAP authentication via Basic Authentication (BasicAuth).
#
# This can be helpful in situations where an application has no authentication,
# no external authentication, or its authentication system is recalcitrant.  Be
# aware that this system doesn't allow for any sort of fine grained permission
# management.  There is either access granted or denied across the entire
# application.  Further development could expand this capability.
################################################################################
{ ... }: {

}
